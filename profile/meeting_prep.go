package profile

// meeting-prep used to be a profile. It is now a *skill* — a reusable workflow
// any identity profile can load on demand. See skills/meeting-prep/SKILL.md.
// The built-in identity profiles (personal, work) live in builtins.go.
